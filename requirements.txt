# Core is stdlib-only. Optional runtime dependencies:
# ccxt  >= 4.0        # live crypto feed (pip install ccxt)
# MetaTrader5 >= 5.0  # live MT5 execution (Windows, with a running terminal)
# Dev/testing:
pytest
